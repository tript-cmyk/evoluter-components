export enum TAG_SIZE {
  DESKTOP = "desktop",
  MOBILE = "mobile",
}

export enum TAG_VARIANT {
  DEFAULT = "default",
  COLOR_GREEN = "colorGreen",
  COLOR_ORANGE = "colorOrange",
  GLASS = "glass",
}

export enum TAG_SHOW_ICON_POSITION {
  NONE = "none",
  LEFT = "left",
  RIGHT = "right",
  BOTH = "both",
}
