export enum TAB_ICON_POSITION {
  LEFT = "left",
  RIGHT = "right",
  BOTH = "both",
  ONLY = "only",
  NONE = "none",
}

export enum TAB_ITEM_POSITION {
  FIRST = "first",
  MIDDLE = "middle",
  LAST = "last",
  ONLY = "only",
}